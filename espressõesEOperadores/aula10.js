// Operadores logicos (Logical operator)

// 2 valores booleanos, que quando verificados,
// resultará em verdadeiro ou falso.

let pao = true;
let queijo = true;

// AND && 
console.log(pao && queijo);

// OR ||
console.log(pao || queijo);

//NOT !
console.log(!queijo); // NEGANDO/TROCANDO O VALOR